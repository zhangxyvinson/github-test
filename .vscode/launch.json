{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug main (lldb)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main",
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",
      "preLaunchTask": "build (clang++)"
    }
  ]
}